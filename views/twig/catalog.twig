<div class="catalog">
    {% for value in product %}
    <div class="cat-product">
        <h1>{{ value.name }}</h1>
        <p>{{ value.description }}</p>
        <p>$ {{ value.price }}</p>
    </div>
    {% endfor %}
</div>